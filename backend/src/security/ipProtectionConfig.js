const CONFIG = {
    MAX_REQUESTS_PER_SECOND: 100,
    MAX_REQUESTS_PER_MINUTE: 1000,
    MAX_REQUESTS_PER_HOUR: 10000,
    SUSPICIOUS_THRESHOLD: 100,
    BLOCK_THRESHOLD: 200,
    PERMANENT_BLOCK_THRESHOLD: 500,
    TEMP_BLOCK_DURATION: 2 * 60 * 1000,
    MEDIUM_BLOCK_DURATION: 15 * 60 * 1000,
    LONG_BLOCK_DURATION: 60 * 60 * 1000,
    CLEANUP_INTERVAL: 5 * 60 * 1000,
    MAX_IP_RECORDS: 10000,
    MAX_BLOCKED_IPS: 5000,
    AUTO_BLOCK_ENABLED: false
};

export {
    CONFIG
};
