# Secrets / environment — NEVER commit
.env
.env.*
*.env
*.bak
*.bak.*
backend/.env
backend/.env.*

# Database backups & dumps (contain secrets + PII; the .tar.gz bundles include .env)
backups/
*.sql
*.sql.gz
*.tar.gz

# Logs
*.log

# Misc recovery artifacts
photorec.log
